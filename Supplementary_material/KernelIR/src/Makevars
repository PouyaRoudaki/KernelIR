PKG_CXXFLAGS = -O3 $(SHLIB_OPENMP_CXXFLAGS) -DARMA_NO_DEBUG
ifeq ($(OS),Windows_NT)
  PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) -L"$(R_HOME)/bin/x64" -lRlapack -lRblas -larmadillo
else
  PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) -llapack -lblas -lgfortran -larmadillo
endif

